{
  "name": "epson-receipts",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "(cd domain && npm install) && (cd client && npm install) && (cd server && npm install)"
  },
  "dependencies": {
    "express": "^3.4.8"
  }
}
