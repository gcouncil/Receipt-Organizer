// TODO: Test me!

//var angular = require('angular');
//var expect = require('chai').expect;

//describe('reports list view directive', function() {

  //beforeEach(function() {
    //var ctx = this;
    //ctx.reportStorage = {};
    //ctx.itemStorage = {};

    //angular.mock.module('ngMock', 'epsonreceipts.reports-list-view', {
      //reportStorage: ctx.reportStorage,
      //itemStorage: ctx.itemStorage
    //});

    //angular.mock.inject(function($rootScope, $compile, $controller) {
      //ctx.scope = $rootScope.$new();

      //ctx.compile = function() {
        //ctx.element = $compile('<reports-list-view reports="reports"></reports-list-view>')(ctx.scope);

        //ctx.scope.items = [
          //{ id: 1, total: 10 }
        //];

        //ctx.scope.reports = [{
          //name: 'report1',
          //items: [1, 2, 3]
        //}];

        //ctx.scope.$digest();
      //};
    //});
    //ctx.compile();
  //});

  //afterEach(function() {
    //var ctx = this;
    //if (ctx.scope) {
      //ctx.scope.$destroy();
    //}
  //});

  //it('should set reports', function() {
    //var ctx = this;
    //expect(ctx.scope.reports.length).to.equal(1);
  //});
//});
